/**
 * Generated by orval v6.8.1 🍺
 * Do not edit manually.
 * DEV API (beta)
 * 
Access Forem articles, users and other resources via API.

For a real-world example of Forem in action, check out [DEV](https://www.dev.to).

All endpoints that don't require authentication are CORS enabled.

All requests must send a user-agent header.

Dates and date times, unless otherwise specified, must be in
the [RFC 3339](https://tools.ietf.org/html/rfc3339) format.

 * OpenAPI spec version: 0.9.7
 */
import useSwr, { SWRConfiguration, Key } from 'swr';
import type { Follower, APIError, GetFollowersParams } from '.././models';
import { customInstance } from '../../orval/custom-instance';

type AwaitedInput<T> = PromiseLike<T> | T;

type Awaited<O> = O extends AwaitedInput<infer T> ? T : never;

/**
 * This endpoint allows the client to retrieve a list of the followers
they have.

"Followers" are users that are following other users on the website.

It supports pagination, each page will contain `80` followers by default.

 * @summary Followers
 */
export const getFollowers = (params?: GetFollowersParams) => {
  return customInstance<Follower[]>({
    url: `/followers/users`,
    method: 'get',
    params,
  });
};

export const getGetFollowersKey = (params?: GetFollowersParams) => [
  `/followers/users`,
  ...(params ? [params] : []),
];

export type GetFollowersQueryResult = NonNullable<
  Awaited<ReturnType<typeof getFollowers>>
>;
export type GetFollowersQueryError = APIError;

export const useGetFollowers = <TError = APIError>(
  params?: GetFollowersParams,
  options?: {
    swr?: SWRConfiguration<Awaited<ReturnType<typeof getFollowers>>, TError> & {
      swrKey: Key;
    };
  }
) => {
  const { swr: swrOptions } = options ?? {};

  const swrKey = swrOptions?.swrKey ?? (() => getGetFollowersKey(params));
  const swrFn = () => getFollowers(params);

  const query = useSwr<Awaited<ReturnType<typeof swrFn>>, TError>(
    swrKey,
    swrFn,
    swrOptions
  );

  return {
    swrKey,
    ...query,
  };
};
